

  4. Factory Methods không nhất quán

  - IdVO có cả init(), fromValue(), và create() - gây nhầm lẫn
  - OrderId thiếu override các static methods

  5. Business Logic rò rỉ

  - order.aggregate.ts:86-87 - So sánh ID dùng string thay vì Value Object equals
  - order-item.entity.ts:30-39 - Validation logic nên ở constructor

  6. Thiếu Domain Events quan trọng

  - Order không emit events khi thay đổi status
  - OrderItem không emit events khi update
